
Why that matters
- The workflow file has a YAML syntaxyaml
name: Generate package-lock PR
on:
  workflow_dispatch:

jobs:
  generate-lock-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v error (line 22). Until that is fixed GitHub won’t let you run the workflow manually and any runs will fail immediately.

Quick fix — replace the broken workflow with a known-good file
1. Open .github/work4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name:flows Install/g dependenciesenerate (-lockgenerate-pr lock.ymlfile in)
 the        Git runHub: web npm UI install.


2     . - Click name Edit: ( Createp pullencil icon) and replace the entire file with this corrected YAML (copy‑paste exactly):

```yaml
name: Generate package-lock PR
on:
  workflow_dispatch:

jobs:
  generate-lock request with package-lock.json
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: chore(ci): add package-lock.json
          branch: add-package-lock-${{ github.run_id }}
          title: Add package-lock.json
          body: This PR adds package-lock.json generated by workflow.
          base: main
         -pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies (generate lockfile)
        run: npm paths: |
            package-lock.json
